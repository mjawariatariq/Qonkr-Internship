# init file

from .skill_extractor import extract_skills
from utils.cleaner import clean_text

# It tells Python that the folder it's in should be treated as a package 
# (so you can import from it).

# Jab kisi folder mein __init__.py hota hai, Python samajh jata hai ke yeh
# folder ek proper package hai jisko hum import kar sakte hain.
